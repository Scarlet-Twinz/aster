# ASTER

**General-purpose programming language and compiler built from scratch in Rust.**

ASTER is a compact compiler project designed to demonstrate the full path from source code to execution rather than stopping at tokenization or syntax parsing.

> **Portfolio project:** ASTER is a focused compiler-engineering project for local evaluation. It is intentionally small and coherent, with documented features backed by implementation and tests.

---

## Product Preview

ASTER demonstrates a complete educational compiler pipeline:

```text
Source Code
    │
    ▼
  Lexer
    │
    ▼
  Tokens
    │
    ▼
  Parser
    │
    ▼
   AST
    │
    ▼
Semantic Analysis
    │
    ▼
  Bytecode
    │
    ▼
Virtual Machine
```

The project exposes the intermediate representations directly through AST and bytecode inspection commands, making the compiler behavior easier to understand and evaluate.

---

## Core Features

### Language foundation

- Token model and lexer
- Recursive-descent parser with operator precedence
- Abstract syntax tree
- Variables and assignments
- Explicit variable type annotations
- Blocks and conditional statements
- Functions, parameters, calls, and returns
- Lexical scopes

### Semantic analysis

- `number`, `string`, `bool`, `void`, and `unknown` types
- Duplicate declaration detection
- Undefined variable and function detection
- Function arity checking
- Duplicate parameter detection
- Operator type checking
- Assignment type checking
- Explicit annotation validation
- Non-boolean conditional validation

### Execution engine

- Bytecode instruction set
- Bytecode compiler
- Stack-based virtual machine
- Global variables
- Local variable slots
- Function call frames
- Recursive function calls
- Conditional jumps
- Runtime values
- Bytecode disassembler

### Developer experience

- `--check` semantic validation
- `--dump-ast` AST inspection
- `--dump-bytecode` bytecode inspection
- `--repl` interactive mode
- Structured compiler errors
- Parser/lexer source locations
- Regression tests across frontend, semantics, execution, and disassembly
- GitHub Actions CI

---

## Example

```text
let answer: number = 40 + 2;

if answer > 0 {
    print(answer);
}
```

Running the program produces:

```text
42
```

ASTER also supports explicit types:

```text
let answer: number = 42;
let message: string = "hello";
let enabled: bool = true;
```

A mismatched annotation is rejected during semantic analysis before bytecode generation.

---

## CLI

Run a program:

```bash
cargo run -- examples/hello.aster
```

Check semantics without executing:

```bash
cargo run -- --check examples/hello.aster
```

Inspect the AST:

```bash
cargo run -- --dump-ast examples/hello.aster
```

Inspect generated bytecode:

```bash
cargo run -- --dump-bytecode examples/hello.aster
```

Start the REPL:

```bash
cargo run -- --repl
```

Inside the REPL:

```text
aster> :help
aster> :quit
```

---

## Architecture

```text
                         ┌─────────────────────┐
                         │     ASTER Source     │
                         └──────────┬──────────┘
                                    │
                                    ▼
                         ┌─────────────────────┐
                         │        Lexer        │
                         └──────────┬──────────┘
                                    │
                                    ▼
                         ┌─────────────────────┐
                         │       Parser        │
                         └──────────┬──────────┘
                                    │
                                    ▼
                         ┌─────────────────────┐
                         │        AST          │
                         └──────────┬──────────┘
                                    │
                                    ▼
                         ┌─────────────────────┐
                         │ Semantic Analyzer   │
                         └──────────┬──────────┘
                                    │
                                    ▼
                         ┌─────────────────────┐
                         │ Bytecode Compiler   │
                         └──────────┬──────────┘
                                    │
                                    ▼
                         ┌─────────────────────┐
                         │  Stack VM + Frames  │
                         └─────────────────────┘
```

The implementation uses Rust-managed values and call frames. ASTER therefore exposes a real stack-machine execution model while relying on Rust for memory safety in the compiler and runtime implementation.

---

## Tech Stack

| Layer | Technology |
| --- | --- |
| Language / Compiler | Rust |
| Compiler frontend | Custom lexer + recursive-descent parser |
| Intermediate representation | Custom AST |
| Semantic layer | Custom type and scope checker |
| Execution | Custom bytecode VM |
| Runtime | Rust |
| Testing | Rust integration tests |
| CI | GitHub Actions |
| Build system | Cargo |

---

## Repository Structure

```text
aster/
├── src/
│   ├── ast.rs            # Abstract syntax tree
│   ├── bytecode.rs       # Bytecode compiler and instructions
│   ├── disassembler.rs   # Bytecode inspection
│   ├── lexer.rs          # Source tokenization
│   ├── lib.rs            # Compiler pipeline and public API
│   ├── main.rs           # CLI and REPL
│   ├── parser.rs         # Recursive-descent parser
│   ├── semantic.rs       # Type and scope analysis
│   ├── token.rs          # Token definitions
│   ├── type_system.rs    # ASTER types
│   └── vm.rs             # Stack-based virtual machine
│
├── examples/
│   └── hello.aster
├── tests/
│   ├── disassembler.rs
│   ├── execution.rs
│   ├── frontend.rs
│   └── semantic.rs
├── docs/
│   └── architecture.md
├── .github/
│   └── workflows/ci.yml
├── Cargo.toml
└── README.md
```

---

## Getting Started

### Prerequisites

Install a current stable Rust toolchain with Cargo.

Verify:

```bash
rustc --version
cargo --version
```

### Clone and build

```bash
git clone https://github.com/Scarlet-Twinz/aster.git
cd aster
cargo build
```

### Run the example

```bash
cargo run -- examples/hello.aster
```

---

## Testing & Quality

Run formatting:

```bash
cargo fmt --all
```

Run the complete test suite:

```bash
cargo test --all-targets --all-features
```

Run Clippy with warnings treated as errors:

```bash
cargo clippy --all-targets --all-features -- -D warnings
```

Build an optimized release binary:

```bash
cargo build --release --all-features
```

The repository's GitHub Actions workflow runs the same core quality gates on pushes and pull requests: formatting, Clippy, tests, and a release build.

---

## Current Status

**ASTER v0.1 — complete and portfolio-ready.**

The v0.1 scope includes a working lexer, parser, AST, semantic analyzer, bytecode compiler, virtual machine, functions and recursion, explicit variable typing, disassembly, CLI tooling, REPL support, regression tests, documentation, and CI.

The project deliberately does **not** claim features that are outside this milestone. Future language work could include modules/imports, richer function type signatures, closures, a larger standard library, and more advanced runtime facilities.

---

## Engineering Focus

ASTER was built to demonstrate compiler and systems engineering fundamentals:

- language design and parsing;
- intermediate representation design;
- semantic validation and lexical scoping;
- bytecode generation;
- virtual-machine execution;
- function call frames and recursion;
- compiler diagnostics;
- test-driven regression coverage;
- Rust systems programming.

The goal is not maximum language complexity. The goal is a small compiler where each documented feature has a concrete implementation path from source code to execution.

---

## License

MIT

## Author

**Anthony Emmanuella Mmasinachi**

Full-stack developer focused on frontend engineering, backend systems, APIs, automation, databases, realtime applications, and practical software architecture — with ASTER extending that portfolio into compiler and systems engineering.

**GitHub:** https://github.com/Scarlet-Twinz
